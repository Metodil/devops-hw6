route:
  group_by: ['alertname']
  receiver: smtp-local
receivers:
  - name: 'smtp-local'
    email_configs:
    - to: 'metodil@hotmail.com'
      from: 'devopv@lab.com'
      require_tls: false
      smarthost: localhost:25
      send_resolved: true
